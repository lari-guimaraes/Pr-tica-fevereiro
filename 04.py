def get_midlle(s):
    lista = []
    lista = list(s)
    metade = int((len(lista) // 2))
    i = 0
    if ((len(lista) % 2) == 0):
        for i in range(len(lista)):


    else:
        lista_middle = lista[metade]
    print(lista_middle)
    print(lista)
    print(len(lista))

palavra = input('Digite uma palavra: ')
get_midlle(palavra)